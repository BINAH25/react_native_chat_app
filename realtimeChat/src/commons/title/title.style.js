import { StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    text:{
        color:'white',
        textAlign: 'center',
        fontSize: 48,
        marginHorizontal: 20,
    }
})
export default styles
